# Security

Report security issues privately to the repository owner.

Do not include secrets, private prompts, thread logs, or session transcripts in public issues. The thread manager stores local runtime state under `~/.pi/agent/thread-manager/`; treat those files as local operational data, not publishable project artifacts.
