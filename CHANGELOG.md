# Changelog

## 0.1.0 - Initial public seed

- Add daemon-backed Pi thread manager extension with `/threads` command and `thread` tool.
- Add durable broker state, child Pi RPC lifecycle management, worktree isolation, approvals, scheduler primitives, and GitHub review-loop support.
